二つのファイルからの集計
========================

内容
----
* 添付の input1.csv ファイルと input2.csv ファイルを入力して、それぞれの Key 列に対する Value 列の値の平均を計算してください。
* 計算した2つの平均値を Key の値で紐付けて一行にし、それぞれ Average1 行、Average2 行に出力してください。
* つまり Average1 行には input1.csv からの平均が、Average2 行には input2.csv からの平均が出力されます。
* さらに Average1 行と Average2 行の値を平均を取った TotalAverage という行も出力してください。
* 作成するコードは以下のコマンドで実行して、今度は result.csv ファイルに出力されるようにしてください。
* この時 Key の値でソートされるようにしてください。
* 内容が、result-sample.csv と一致すればOKです。