基本的な集計
============

内容
----
* 添付の input1.csv ファイルを入力として、Key 列に対する Value 列の値の平均を画面に表示してください
* 作成するコードは以下のコマンドで実行して、画面に result-sample.csv と同一の内容が表示されればOKです
* Key でのソートはしないので、実行環境によっては行の順番が一致しないかもしれないです
